#!/usr/bin/env python3
"""Install agtask command hooks without replacing unrelated handlers."""

from pathlib import Path
import os
import sys


CLI = Path(__file__).resolve().with_name("agtask")
os.execv(sys.executable, [sys.executable, str(CLI), "install-hooks", *sys.argv[1:]])
