#!/usr/bin/env python3
"""Remove only agtask command-hook groups."""

from pathlib import Path
import os
import sys


CLI = Path(__file__).resolve().with_name("agtask")
os.execv(sys.executable, [sys.executable, str(CLI), "uninstall-hooks", *sys.argv[1:]])
