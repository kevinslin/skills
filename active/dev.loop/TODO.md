### make this skill configuable

read config file at
```
$PROJECT_ROOT/.llm/dev.loop/config.yaml
```

```yaml
# override phase instructions
override:
  goal:
    instructions: # new instructions for phase 1
    

# modify configurable parts of each phase
configuration:
  goal:
  polish:
    documentation:
```