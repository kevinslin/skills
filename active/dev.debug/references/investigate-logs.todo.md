# investigate-logs TODO

Use this file to expand the `investigate-logs` workflow in `SKILL.md`.

## Fill In Later

- Canonical clean-log command if `scripts/make_clean_log.sh` should be replaced.
- Service-specific parsing heuristics (multi-line traces, JSON log fields, known noise lines).
- A standard triage checklist (symptom -> evidence -> likely root cause).
- Escalation rules for when to request more logs or reproduction steps.
