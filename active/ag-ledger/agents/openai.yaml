interface:
  display_name: "AG Ledger"
  short_description: "Sync and query agent activity logs"
  default_prompt: "Use $ag-ledger to sync recent Codex conversations into the local activity ledger or inspect prior ledger entries."
